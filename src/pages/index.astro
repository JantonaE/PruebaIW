---
import Container from "../components/container.astro";
import IndexText from "../components/IndexText.astro";
import Layout from "../layouts/Layout.astro";
import Map from "../components/Map.svelte";

//import * as lib from "../lib/mongo/house";
import * as lib from "../lib/mongo/eventual";
const data = await lib.findAllEventuals();
console.log(data);

---

<Layout title="">
  <Container>
    <IndexText />
    <Map prods={data} client:only />
  </Container>
</Layout>
